:layout: tutorial-only-right

Tutorial Only Right-aligned
###########################

To set the page's layout to the tutorial only, right-aligned layout set the following configuration settings in the page:

* :code:`:layout: tutorial-only-right`
